# Optional shell settings. This reference file is not automatically loaded.
BOOK_INDEX_DIR=data/index_metadata
BOOK_METADATA_PATH=data/metadata.csv
BOOK_MODEL_NAME=intfloat/multilingual-e5-base
BOOK_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
# Explicit executable overrides PATH and the Windows fallback.
TESSERACT_CMD=
# Optional: improves Hugging Face download limits; never required.
HF_TOKEN=
